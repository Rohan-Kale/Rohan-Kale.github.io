# Draft projects

These entries are saved for later and are not included in the generated website.

To restore PrivateAI Gateway, copy the object in `privateai-gateway.json` into the `projects` array in `../portfolio.json`. Add its source-code URL, remove `status` and `codePending` when appropriate, and run `npm run content`.
